// This file was generated by SquareLine Studio
// SquareLine Studio version: SquareLine Studio 1.5.0
// LVGL version: 9.1.0
// Project name: Wires_Project

#include "ui.h"
#include "ui_helpers.h"

///////////////////// VARIABLES ////////////////////


// SCREEN: ui_Main
void ui_Main_screen_init(void);
lv_obj_t * ui_Main;
lv_obj_t * ui_barWire1;
void ui_event_swSelect1(lv_event_t * e);
lv_obj_t * ui_swSelect1;
lv_obj_t * ui_barWire2;
void ui_event_swSelect2(lv_event_t * e);
lv_obj_t * ui_swSelect2;
lv_obj_t * ui_barWire3;
void ui_event_swSelect3(lv_event_t * e);
lv_obj_t * ui_swSelect3;
lv_obj_t * ui_barWire4;
void ui_event_swSelect4(lv_event_t * e);
lv_obj_t * ui_swSelect4;
lv_obj_t * ui_barWire5;
void ui_event_swSelect5(lv_event_t * e);
lv_obj_t * ui_swSelect5;
lv_obj_t * ui_barWire6;
void ui_event_swSelect6(lv_event_t * e);
lv_obj_t * ui_swSelect6;
lv_obj_t * ui_imgResult;
void ui_event_sldBrightness(lv_event_t * e);
lv_obj_t * ui_sldBrightness;
// CUSTOM VARIABLES

// EVENTS
lv_obj_t * ui____initial_actions0;

// IMAGES AND IMAGE SETS

///////////////////// TEST LVGL SETTINGS ////////////////////
#if LV_COLOR_DEPTH != 16
    #error "LV_COLOR_DEPTH should be 16bit to match SquareLine Studio's settings"
#endif

///////////////////// ANIMATIONS ////////////////////

///////////////////// FUNCTIONS ////////////////////
void ui_event_swSelect1(lv_event_t * e)
{
    lv_event_code_t event_code = lv_event_get_code(e);
    lv_obj_t * target = lv_event_get_target(e);

    if(event_code == LV_EVENT_VALUE_CHANGED &&  !lv_obj_has_state(target, LV_STATE_CHECKED)) {
        _ui_state_modify(ui_swSelect1, LV_STATE_DISABLED, _UI_MODIFY_STATE_ADD);
        OnWireSelect(e);
    }
}

void ui_event_swSelect2(lv_event_t * e)
{
    lv_event_code_t event_code = lv_event_get_code(e);
    lv_obj_t * target = lv_event_get_target(e);

    if(event_code == LV_EVENT_VALUE_CHANGED &&  !lv_obj_has_state(target, LV_STATE_CHECKED)) {
        _ui_state_modify(ui_swSelect2, LV_STATE_DISABLED, _UI_MODIFY_STATE_ADD);
        OnWireSelect(e);
    }
}

void ui_event_swSelect3(lv_event_t * e)
{
    lv_event_code_t event_code = lv_event_get_code(e);
    lv_obj_t * target = lv_event_get_target(e);

    if(event_code == LV_EVENT_VALUE_CHANGED &&  !lv_obj_has_state(target, LV_STATE_CHECKED)) {
        _ui_state_modify(ui_swSelect3, LV_STATE_DISABLED, _UI_MODIFY_STATE_ADD);
        OnWireSelect(e);
    }
}

void ui_event_swSelect4(lv_event_t * e)
{
    lv_event_code_t event_code = lv_event_get_code(e);
    lv_obj_t * target = lv_event_get_target(e);

    if(event_code == LV_EVENT_VALUE_CHANGED &&  !lv_obj_has_state(target, LV_STATE_CHECKED)) {
        _ui_state_modify(ui_swSelect4, LV_STATE_DISABLED, _UI_MODIFY_STATE_ADD);
        OnWireSelect(e);
    }
}

void ui_event_swSelect5(lv_event_t * e)
{
    lv_event_code_t event_code = lv_event_get_code(e);
    lv_obj_t * target = lv_event_get_target(e);

    if(event_code == LV_EVENT_VALUE_CHANGED &&  !lv_obj_has_state(target, LV_STATE_CHECKED)) {
        _ui_state_modify(ui_swSelect5, LV_STATE_DISABLED, _UI_MODIFY_STATE_ADD);
        OnWireSelect(e);
    }
}

void ui_event_swSelect6(lv_event_t * e)
{
    lv_event_code_t event_code = lv_event_get_code(e);
    lv_obj_t * target = lv_event_get_target(e);

    if(event_code == LV_EVENT_VALUE_CHANGED &&  !lv_obj_has_state(target, LV_STATE_CHECKED)) {
        _ui_state_modify(ui_swSelect6, LV_STATE_DISABLED, _UI_MODIFY_STATE_ADD);
        OnWireSelect(e);
    }
}

void ui_event_sldBrightness(lv_event_t * e)
{
    lv_event_code_t event_code = lv_event_get_code(e);

    if(event_code == LV_EVENT_VALUE_CHANGED) {
        OnBrightnessChange(e);
    }
}

///////////////////// SCREENS ////////////////////

void ui_init(void)
{
    lv_disp_t * dispp = lv_display_get_default();
    lv_theme_t * theme = lv_theme_default_init(dispp, lv_palette_main(LV_PALETTE_BLUE), lv_palette_main(LV_PALETTE_RED),
                                               false, LV_FONT_DEFAULT);
    lv_disp_set_theme(dispp, theme);
    ui_Main_screen_init();
    ui____initial_actions0 = lv_obj_create(NULL);
    lv_disp_load_scr(ui_Main);
}
